tenant_id=
client_id=
client_secret=
target_user_id=
# Optional local SOAR SDK test asset ID. The test harness uses this as the
# simulated asset identity and as the salt when encrypting client_secret.
SOAR_ASSET_ID=123
# Optional local SOAR SDK test container ID. Download tests attach files to
# this simulated container in the SDK's local vault implementation.
SOAR_CONTAINER_ID=456
